// lib/screens/instant_screen.dart

import 'package:flutter/material.dart';

class InstantScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Instant Content', style: TextStyle(fontSize: 24)),
    );
  }
}