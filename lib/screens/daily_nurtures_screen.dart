// lib/screens/daily_nurtures_screen.dart

import 'package:flutter/material.dart';

class DailyNurturesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Daily Nurtures Content',
        style: TextStyle(fontSize: 24, color: Colors.white), // Set text color
      ),
    );
  }
}