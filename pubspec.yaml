name: goldenminds
description: "The Golden Minds App for your hollistic well being"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  flutter_signin_button: ^2.0.0  # Ensure you're using the correct package
  cupertino_icons: ^1.0.8
  multi_select_flutter: ^4.0.0
  speech_to_text: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true